# copy to .env and adjust
DATABASE_URL=postgres://cartobase:cartobase@localhost:5432/cartobase
BIND_ADDR=0.0.0.0:8080
# optional: bootstrap an admin token on first run (printed to logs if omitted)
CARTOBASE_ADMIN_TOKEN=
CARTOBASE_ADMIN_CREW=default
RUST_LOG=cartobase=info,tower_http=warn
